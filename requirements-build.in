-r requirements.in

# Uniquement pour construire le bundle (build.py, profil ordinaire) : jamais
# nécessaires au bundle une fois figé, PyInstaller les laisse hors du .exe.
pyinstaller
Pillow
pystray
